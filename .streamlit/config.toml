[client]
showSidebarNavigation = false

[theme]
# Primary accent color (Electric Indigo)
primaryColor = "#6366F1"

# Background color (Deep space navy)
backgroundColor = "#0D0F17"

# Secondary background color (Surface/card bg)
secondaryBackgroundColor = "#13151F"

# Text color (White/slate)
textColor = "#F1F5F9"

# Font (Inter is default sans-serif for clean look)
font = "sans serif"
